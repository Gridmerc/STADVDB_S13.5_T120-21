

SELECT      client_name, age, marital_status
FROM        CLIENTS
WHERE       age > 0;

SELECT      client_name, age, marital_status
FROM        CLIENTS
WHERE       age < 0;

SELECT      client_name, age, marital_status
FROM        CLIENTS
WHERE       age = 46;

SELECT      client_name, age, marital_status
FROM        CLIENTS
WHERE       age = 73;
