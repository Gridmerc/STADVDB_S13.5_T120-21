Create index idx_cards_card_id on cards(card_id);

Create index idx_cards_card_type on cards(card_type);

Create index idx_clients_clientID on clients(clientID);

Create index idx_clients_age on clients(age);

Create index idx_clients_dependent_count on clients(dependent_count);

Create index idx_clients_marital_status on clients(marital_status);

Create index idx_credit_infos_clientID on credit_infos(clientID);

Create index idx_education_levels_education_id on education_levels(education_id);

Create index idx_education_levels_education_type on education_levels(education_type);

Create index idx_incomes_income_id on incomes(income_id);

Create index idx_incomes_income_type on incomes(income_type);



